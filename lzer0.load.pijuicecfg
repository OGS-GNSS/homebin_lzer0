#!/bin/bash
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
source "${SCRIPT_DIR}/lzer0.config.sh"

python3 "${LZERO_BIN_DIR}/pijuice_util.py" --load < "${LZERO_CONFIG_DIR}/pijuice/pijuice_settings.json"
