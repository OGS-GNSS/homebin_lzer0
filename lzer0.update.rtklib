#!/bin/bash

# Copia il repository Git in /tmp
git clone git@github.com:rtklibexplorer/RTKLIB.git /home/lzer0/tmp/RTKLIB

# Lancio del Makefile
cd /home/lzer0/tmp/RTKLIB/app/consapp
make
pwd
# Eliminazione del repository
rm -rf /home/lzer0/tmp/RTKLIB

